<%= form_for(@user_summary) do |f| %>
  <% if @user_summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_summary.errors.count, "error") %> prohibited this user_summary from being saved:</h2>

      <ul>
      <% @user_summary.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br>
    <%= f.text_field :company_id %>
  </div>
  <div class="field">
    <%= f.label :exp %><br>
    <%= f.number_field :exp %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
